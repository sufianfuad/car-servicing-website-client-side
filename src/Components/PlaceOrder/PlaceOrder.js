import React from 'react';

const PlaceOrder = () => {
    return (
        <section>
            <h2 className='text-center'>Place Your Order</h2>
        </section>
    );
};

export default PlaceOrder;